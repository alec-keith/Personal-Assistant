# Core AI
anthropic>=0.40.0

# Discord (voice requires PyNaCl + imageio-ffmpeg, both installed)
discord.py[voice]>=2.3.0
imageio-ffmpeg>=0.5.0

# Voice transcription (Groq Whisper API — free tier)
groq>=0.15.0

# Todoist
httpx>=0.27.0

# Calendar (iCloud CalDAV)
caldav>=1.3.9
icalendar>=5.0.13

# Memory
chromadb>=0.5.0

# Scheduler
APScheduler>=3.10.0

# Config
pydantic-settings>=2.0.0
pydantic>=2.0.0

# HTTP server (BlueBubbles webhook receiver + health endpoint)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Utilities
python-dotenv>=1.0.0
