# Core AI
anthropic>=0.40.0

# Discord — using py-cord (discord.py fork) which includes discord.sinks for voice receiving
# discord.py 2.x removed sinks; py-cord 2.x has the full API including start_recording
py-cord[voice]>=2.4.0
imageio-ffmpeg>=0.5.0

# Voice transcription (Groq Whisper API — free tier)
groq>=0.15.0

# Todoist
httpx>=0.27.0

# Calendar (iCloud CalDAV)
caldav>=1.3.9
icalendar>=5.0.13

# Memory (PostgreSQL + pgvector)
asyncpg>=0.30.0
voyageai>=0.3.0

# Scheduler
APScheduler>=3.10.0

# Config
pydantic-settings>=2.0.0
pydantic>=2.0.0

# HTTP server (BlueBubbles webhook receiver + health endpoint)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Web search + page fetch (Tavily)
tavily-python>=0.5.0

# Weather (Open-Meteo — no key needed, uses httpx already in deps)
# News (NewsAPI — requires NEWSAPI_KEY)
# Stocks (Yahoo Finance — no key needed)
yfinance>=0.2.0

# TTS — OpenAI (primary, requires OPENAI_API_KEY) + edge-tts (free fallback)
openai>=1.0.0
edge-tts>=6.1.0

# Email (Gmail REST API + Yahoo IMAP — Yahoo uses stdlib imaplib, no extra dep)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0

# Utilities
python-dotenv>=1.0.0
